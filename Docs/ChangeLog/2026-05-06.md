# SAVI Change Log - 2026-05-06

## TestFlight Production ID Setup

- Registered the production Apple Developer identifiers under team
  `ANDREUS BELLO-LIF - 887JW5T2M8`.
- `com.savi.app` was unavailable, so the production Release channel now uses
  `com.altatecrd.savi`.
- Created/verified:
  - App ID: `com.altatecrd.savi`
  - Share extension App ID: `com.altatecrd.savi.ShareExtension`
  - App Group: `group.com.altatecrd.savi.shared`
  - iCloud container: `iCloud.com.altatecrd.savi`
- Created the App Store Connect app record as `SAVI: Save Anything` with bundle
  ID `com.altatecrd.savi`; the installed display name remains `SAVI`.
- Updated local Release signing, entitlements, App Group support, CloudKit
  container references, install scripts, and release docs to the new production
  namespace.
- Verified Release generic iPhoneOS build succeeds with
  `CODE_SIGNING_ALLOWED=NO`.
- Verified signed Release iPhoneOS build succeeds with
  `-allowProvisioningUpdates`, creating provisioning profiles for the app and
  share extension.
- Created signed archive:
  `/Users/guest1/Desktop/SAVI_QA/Archives/SAVI-20260506-132228.xcarchive`.
- Uploaded the archive to App Store Connect with `xcodebuild -exportArchive`;
  upload succeeded and App Store Connect began processing version `1.0`
  build `3`.
- Created internal TestFlight group `SAVI Internal` with automatic access to
  uploaded builds; it currently has `0` testers until developer/team emails are
  added.
- Added build-level `What to Test` instructions for version `1.0` build `3`.

## TestFlight Internal Build 4

- Bumped `CURRENT_PROJECT_VERSION` from `3` to `4`.
- Verified `git diff --check` passes.
- Verified Release iPhoneOS no-sign build succeeds.
- Created signed archive:
  `/Users/guest1/Desktop/SAVI_QA/Archives/SAVI-20260506-143531-b4.xcarchive`.
- Uploaded version `1.0` build `4` to App Store Connect; upload succeeded and
  App Store Connect processed the build.
- Confirmed build `1.0 (4)` is `Complete` / `Ready to Submit` and attached to
  internal group `SAVI Internal`.
- Saved build-level `What to Test` notes for build `4`.
- Added active account holder `altatecrd@gmail.com` to `SAVI Internal`; status
  is `Invited`.
- Sent App Store Connect user invitations:
  - `matti.lamminsalo@gmail.com` as `Developer`
  - `andreusbl@icloud.com` as `Developer`
- Sent an additional App Store Connect user invitation:
  - `andreusbl@mac.com` as `Developer`
- Added `matti.lamminsalo@gmail.com` to TestFlight internal group
  `SAVI Internal`; status is `Invited`.
- Apple does not list pending App Store Connect invitations in the internal
  TestFlight group picker; after each invite is accepted, add that user to
  `SAVI Internal`.

## Sample Library V29 Audio File Preview And CPR Slot

- Bumped native sample seed version to `27` so sample-only installs refresh.
- Moved `Building Effective AI Agents` down out of the first Home saves.
- Moved `Hands-only CPR: what to do before help arrives` into the early utility
  slot after the parasite-medication research card.
- Rebuilt `Mom's lasagna sauce voice note` as a generated M4A/Voice Memo file
  thumbnail with a play button, waveform, duration, and filename.
- Upgraded the shared no-thumbnail audio fallback so future audio files render
  as audio file preview cards instead of generic document icons.

## Sample Library V30 Mom Audio Icon Thumbnail

- Bumped native sample seed version to `28`.
- Reworked `Mom's lasagna sauce voice note` again from a document/file preview
  into a full-bleed audio icon thumbnail with a bold gradient, waveform mark,
  play cue, and small `M4A` badge.
- Removed the fake white file-card border treatment so the thumbnail feels more
  like a polished audio/voice-memo icon.

## TestFlight Build 5 - iPhone 11 Startup Hardening

- Investigated reported iPhone 11 startup crash. No iPhone 11 device or recent
  local SAVI crash log was available from the connected machine, so the fix was
  based on the most likely older-device Release startup risk and simulator QA.
- Hardened Release/TestFlight builds by compiling Apple Intelligence /
  `FoundationModels` imports and execution paths only for Debug builds:
  `#if DEBUG && canImport(FoundationModels)`.
- Release `SAVI` and `SAVIShareExtension` now use the local folder/tag
  classifier path instead of linking Apple Intelligence. Debug `SAVI Test` keeps
  the Apple Intelligence code path available for internal testing.
- Verified Release iPhoneOS build succeeds.
- Verified with `otool -L` that neither Release app binary nor share extension
  binary links `FoundationModels.framework`.
- Created and launched a Release build on iPhone 11 simulator
  `SAVI Crash QA iPhone 11`
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`); launch succeeded as
  `com.altatecrd.savi`.
- Bumped `CURRENT_PROJECT_VERSION` from `4` to `5`.
- Created signed archive:
  `/Users/guest1/Desktop/SAVI_QA/Archives/SAVI-20260506-162038-b5.xcarchive`.
- Uploaded version `1.0` build `5` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Desktop/SAVI_QA/Logs/SAVI-archive-20260506-162038-b5.log`
  - `/Users/guest1/Desktop/SAVI_QA/Logs/SAVI-export-upload-20260506-162255-b5.log`

## TestFlight Build 6 - CloudKit Launch Crash Hotfix

- Confirmed build `1.0 (5)` can crash on iPhone 11 simulator after first UI
  render. The crash report points to `CKContainer(identifier:)` during
  `SaviStore.bootstrap()` / `refreshCloudKitStatus()`.
- Added a Release/TestFlight CloudKit gate so the beta build does not create a
  `CKContainer` at launch or from backup actions. Debug/internal builds keep
  the CloudKit path available for development.
- Updated Profile copy and backup controls so TestFlight users use full local
  archive export/import while iCloud backup is paused.
- Bumped `CURRENT_PROJECT_VERSION` from `5` to `6`.
- Verified `git diff --check` passes.
- Verified Release build `6` clean-installs and launches on the iPhone 11
  simulator that reproduced the crash
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no new SAVI crash report.
- Saved launch screenshot:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/savi-build6-iphone11-launch.png`.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-180849-b6.xcarchive`.
- Uploaded version `1.0` build `6` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build6-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build6.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build6.log`

## TestFlight Build 7 - Release CloudKit Link Removal

- Followed up on a reported Matti iPhone 11 / iOS 17.4 launch crash after
  build `1.0 (6)`.
- Could not pull Matti's remote device crash report from this Mac because no
  App Store Connect API key or device connection is available locally, and
  TestFlight crash reports can lag.
- Local simulator runtimes currently include only iOS `26.4`; no iOS 17.4
  simulator runtime is installed on this Xcode setup yet.
- Strengthened the launch hotfix by compiling CloudKit out of Release entirely:
  Release/TestFlight `SAVI` no longer imports or links CloudKit, while
  Debug/internal builds keep CloudKit available for development.
- Kept iCloud backup paused in Release and left full local archive
  export/import as the beta backup path.
- Bumped `CURRENT_PROJECT_VERSION` from `6` to `7`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Verified with `otool -L` that the archived Release app and share extension
  do not link `CloudKit.framework` or `FoundationModels.framework`.
- Built, installed, and launched Release build `7` on the local iPhone 11
  simulator (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no new SAVI crash
  report.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-183214-b7.xcarchive`.
- Uploaded version `1.0` build `7` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build7-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build7-release-iphone11-sim.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build7.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build7.log`

## TestFlight Build 8 - iPhone 11 Launch Hardening

- Followed up on continued reported iPhone 11 / iOS 17 launch crashes after
  build `1.0 (7)`.
- Reviewed local historical crash reports and found two additional
  launch-adjacent crash signatures besides CloudKit:
  - custom `UTType(filenameExtension:)` archive type lookup from
    `NativeSaviRootView`;
  - large data URL thumbnail handling in `SaviText.imageFromDataURL` /
    `SaviText.isSVGDataURL`.
- Hardened launch paths:
  - removed custom archive UTType lookup from root render and changed archive
    import to use `.json` / `.data`;
  - changed data URL thumbnail decoding to inspect only the short MIME header,
    never lowercase/copy the full base64 payload;
  - changed data URL image cache keys so the full data URL is not retained as
    the cache key;
  - added the same bounded data URL decode guard to the share extension preview.
- Added startup breadcrumbs around bootstrap stages to make any future
  TestFlight crash/log easier to pin down.
- Bumped `CURRENT_PROJECT_VERSION` from `7` to `8`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Verified Release build `8` clean-installs and launches on local iPhone 11
  simulator `SAVI Crash QA iPhone 11`
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no new SAVI crash report.
- Saved launch screenshot:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/savi-build8-iphone11-launch.png`.
- Verified with `otool -L` that the build `8` simulator app, archived app, and
  share extension do not link `CloudKit.framework` or
  `FoundationModels.framework`.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-190408-b8.xcarchive`.
- Uploaded version `1.0` build `8` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build8-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build8-release-iphone11-sim.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build8.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build8.log`

## TestFlight Build 9 - Matti iPhone 11 Crash And Search Keyboard Fix

- Downloaded Matti's TestFlight feedback package from App Store Connect:
  `AKVsfNVoFl3QKPzhdu_zuJ4`.
- Saved the feedback and crash report locally:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/matti-build8-crash/feedback.json`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/matti-build8-crash/crashlog.crash`
- Confirmed the real device crash was build `1.0 (8)` on iPhone 11
  (`iPhone12,1`) running iOS `17.4.1`.
- The crash stack pointed to `LinkPresentation` / `WebKit` while
  `SaviMetadataService.fetch(for:waitsForThumbnail:)` was enriching saved-link
  metadata in the background.
- Disabled `LPMetadataProvider` usage in Release/TestFlight metadata fetching
  for both the main app and share extension. Debug keeps the path available only
  on iOS 18+.
- Replaced HTML entity decoding that used `NSAttributedString(data: .html)` with
  a pure Swift decoder in the main app and share extension metadata paths.
- Fixed Search keyboard ergonomics:
  - added keyboard toolbar actions for `Search` and `Done`;
  - Search commits the current query before dismissing the keyboard;
  - scrolling dismisses the keyboard interactively;
  - tapping a Search result while the keyboard is up now dismisses the keyboard
    first instead of accidentally opening an item card.
- Tightened the first-run Share Sheet animation labels so they do not overlap on
  iPhone 11 / small screens.
- Bumped `CURRENT_PROJECT_VERSION` from `8` to `9`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Built, installed, and launched Release build `9` on local iPhone 11 simulator
  `SAVI Crash QA iPhone 11`
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no launch crash.
- Saved launch screenshot:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/savi-build9-iphone11-launch.png`.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-195602-b9.xcarchive`.
- Uploaded version `1.0` build `9` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build9-search-keyboard-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build9-search-keyboard-release-iphone11-sim.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build9-search-keyboard.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build9.log`

## TestFlight Build 10 - iPhone 11 Responsiveness Hotfix

- Investigated Matti's follow-up iPhone 11 report where build `9` opened but
  Home/Search/Explore/Folders felt frozen or very slow while Profile remained
  usable.
- Reduced Release/TestFlight launch work:
  - Release now skips stale metadata refresh on launch, foreground refresh, and
    network reachability changes;
  - Release metadata enrichment concurrency is reduced to one task if any
    explicit enrichment path is triggered later.
- Moved thumbnail work out of SwiftUI render bodies:
  - remote thumbnail image fetch/decode now hops through a detached utility task
    before publishing back to the main actor;
  - large data URL thumbnails now use a bounded cache key and decode through
    `SaviCachedDataURLImage` off the main thread;
  - item thumbnails, folder icons, and folder image backgrounds no longer decode
    base64 images synchronously in `body`.
- Reduced initial Release rendering pressure:
  - Home recent timeline starts with 18 saves instead of 40;
  - Search starts with 24 results instead of 40;
  - Explore snapshots show 16 items instead of 30.
- Bumped `CURRENT_PROJECT_VERSION` from `9` to `10`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Built, installed, and launched Release build `10` on local iPhone 11 simulator
  `SAVI Crash QA iPhone 11`
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no launch crash.
- Confirmed local launch logs show bootstrap skipping stale metadata refresh.
- Saved launch screenshot:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/screenshots/SAVI-build10-iphone11-launch.png`.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-202747-b10.xcarchive`.
- Uploaded version `1.0` build `10` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build10-performance-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build10-performance-release-iphone11-sim.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build10-performance.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build10.log`

## TestFlight Build 11 - Home/Explore Scroll Performance Hotfix

- Followed up on Matti's report that build `10` launched but Home and Explore
  still scrolled slowly on iPhone 11 while Profile stayed responsive.
- Added a Release/TestFlight legacy-device performance mode that applies to all
  beta devices for now, not only iPhone 11.
- Reduced first-paint work:
  - Home recent timeline now starts with fewer visible saves and loads more as
    the user scrolls;
  - Search results start with fewer rows and page in smaller batches;
  - Explore renders a lighter editorial feed in Release and pages in small
    batches instead of building the heavier mosaic up front.
- Reduced row rendering cost:
  - list/timeline rows use a lighter thumbnail view with no `SaviStore`
    environment dependency;
  - Home/Search/Explore "saved ago" labels are static in Release instead of
    creating many per-row `TimelineView` timers;
  - card/folder shadows are lighter in Release performance mode.
- Reduced thumbnail pressure:
  - remote thumbnails are downsampled before caching/display;
  - data URL thumbnails decode off the main actor and are cached by bounded
    keys;
  - offscreen thumbnail loads are cancelled on disappear and delayed slightly
    in Release to prioritize scrolling.
- Promoted `Private Vault` into the first-boot main folder set. Fresh sample
  data now orders the first folders as `Private Vault`, `Life Admin`, `Health`,
  `Watch / Read Later`, and `AI & Work`.
- Bumped `CURRENT_PROJECT_VERSION` from `10` to `11`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Built, installed, and launched Release build `11` on local iPhone 11
  simulator `SAVI Crash QA iPhone 11`
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no launch crash.
- Confirmed fresh simulator data contains `90` sample items and `Private Vault`
  is unlocked/first in folder order.
- Saved launch screenshot:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/screenshots/SAVI-build11-iphone11-launch.png`.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-212111-b11.xcarchive`.
- Uploaded version `1.0` build `11` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build11-performance-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build11-performance-release-sim.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build11-performance.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build11.log`

## TestFlight Build 12 - Native-Only First Launch Performance Hotfix

- Followed up on Matti's report that build `11` still opened but Home/Explore
  movement stayed sluggish on iPhone 11.
- Identified a likely real-device cause from launch behavior: Release builds
  could still mount the hidden `LegacyMigrationHost` `WKWebView` on fresh
  installs or seed-refresh installs, even though the visible UI is native.
  That can spin up WebKit/GPU process work behind Home on older devices.
- Disabled legacy web migration entirely in Release/TestFlight:
  - Debug/internal builds can still use the legacy migration path;
  - Release marks migration complete natively and uses native sample seeding /
    sample refresh instead of a hidden web view.
- Further reduced Release feed pressure:
  - Home starts with 6 recent saves and loads 4 more at a time;
  - Search starts with 10 results and loads 6 more at a time;
  - Explore starts with 4 items and loads 4 more at a time;
  - remote thumbnail fetches are delayed longer so scrolling gets priority;
  - SVG data thumbnails fall back to native placeholders in Release instead of
    creating per-thumbnail `WKWebView`s;
  - the Share Sheet onboarding demo is static in Release performance mode and
    no longer runs its timer animation loop.
- Bumped `CURRENT_PROJECT_VERSION` from `11` to `12`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Built, clean-installed, and launched Release build `12` on local iPhone 11
  simulator `SAVI Crash QA iPhone 11`
  (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`) with no launch crash.
- Confirmed fresh simulator data contains `90` sample items, `migrationComplete`
  is `true`, `legacySeedVersion` is `29`, and first folders are `Private Vault`,
  `Life Admin`, `Health`, `Watch / Read Later`, and `AI & Work`.
- Checked filtered launch logs for `WebKit`, `GPUProcess`, `legacy`, and
  `SAVI Native`; only native bootstrap lines appeared after the fix.
- Saved launch screenshot:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/screenshots/SAVI-build12-iphone11-launch-final.png`.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260506-233205-b12.xcarchive`.
- Uploaded version `1.0` build `12` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build12-webkit-performance-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build12-webkit-performance-release-sim.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build12-webkit-performance.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build12.log`

## TestFlight Build 13 - Adaptive Performance Policy

- Replaced the global Release `legacyDevicePerformanceMode` experience with
  `SaviPerformancePolicy`, selected at runtime by device tier.
- Added hidden QA override support through `SAVI_PERF_TIER=modern|balanced|legacy`
  via launch arguments, environment, or `UserDefaults`.
- Device tiers:
  - `modern`: iPhone 17 / newer-style devices get the rich feed back;
  - `balanced`: iPhone 12-ish / lower-memory edge cases get moderate batches;
  - `legacy`: iPhone 11 family and older get targeted scrolling protections.
- Restored modern behavior:
  - Home initial recent saves `40`, load more `24`;
  - Search initial results `40`, load more `24`;
  - Explore initial items `30`, load more `12`;
  - Explore uses the mosaic board again on modern devices.
- Kept old-phone protections targeted:
  - iPhone 11 logs `perfTier=legacy`;
  - Home initial recent saves `8`, load more `4`;
  - Search initial results `10`, load more `6`;
  - Explore initial items `8`, load more `4`;
  - legacy Explore uses the compact editorial feed.
- Removed fixed thumbnail delays that caused visible pop-in on newer phones.
- Replaced delay-based thumbnail protection with a throttled image work limiter:
  `3` concurrent image jobs on modern, `2` on balanced, `1` on legacy.
- Added short thumbnail cancellation grace to avoid cancel/restart churn during
  fast scrolling.
- Disabled implicit animation when thumbnail placeholders become images.
- Kept Home feed long-press previews disabled to avoid gesture fights while
  preserving tap-to-open behavior; detail/search contexts can still preview.
- Bumped `CURRENT_PROJECT_VERSION` from `12` to `13`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Built and launched Release build `13` on local iPhone 11 simulator
  `SAVI Crash QA iPhone 11` (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`);
  launch succeeded and logs showed `perfTier=legacy`.
- Built and launched Release build `13` on local iPhone 17 simulator
  (`CC9E6699-BC90-42D3-AA84-C82F207B1B25`);
  launch succeeded and logs showed `perfTier=modern`.
- Saved screenshots:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/screenshots/SAVI-build13-iphone11-launch.jpg`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/screenshots/SAVI-build13-iphone17-launch.jpg`
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260507-005800-b13.xcarchive`.
- Uploaded version `1.0` build `13` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build13-adaptive-performance.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build13.log`
