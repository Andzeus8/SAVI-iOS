# 2026-05-08

## Sample Content Safety Guardrail

- Added `Docs/Architecture/SampleContentReview.md` as the living App Store/IP/health-safety review for SAVI's removable sample library.
- Added `scripts/savi-sample-content-check.py` and wired it into `scripts/savi-preflight.sh`.
- The checker validates health disclaimers, NCI counterbalance, remote YouTube metadata posture, fake private-document wording, sample-source documentation, and banned medical-claim wording.
- Updated compliance, App Store submission, CTO handoff, architecture README, sample-source notes, and safety scan docs.

## Third-Party SDK Inventory Guardrail

- Added `Docs/Architecture/ThirdPartySDKInventory.md` to record the current no-third-party-SDK state and the review gate for future Supabase/PostHog/Sentry/RevenueCat/etc. additions.
- Added `scripts/savi-sdk-inventory-check.py` and wired it into `scripts/savi-preflight.sh`.
- Updated compliance, privacy inventory, submission, release, preflight, CTO, architecture, safety scan, and roadmap docs.
- Source anchors used: Apple third-party SDK requirements, privacy manifest files, and user privacy/data-use docs.

## Privacy Manifest Required-Reason API Audit

- Updated `SAVI/PrivacyInfo.xcprivacy` to declare `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`, matching current main-app `UserDefaults` use.
- Kept `NSPrivacyAccessedAPICategoryFileTimestamp` with reason `C617.1` in both app and share extension manifests.
- Added `Docs/Architecture/PrivacyManifestAudit.md` as the current required-reason API audit.
- Added `scripts/savi-privacy-manifest-check.py` and wired it into `scripts/savi-preflight.sh`.
- Updated compliance, submission, preflight, CTO, architecture, worksheet, safety scan, and roadmap docs.
- Source anchors used: Apple required-reason API documentation, Apple privacy manifest files documentation, and TN3183.

## Privacy Data Inventory Guardrail

- Added `Docs/Architecture/PrivacyDataInventory.md` as the living source for App Store privacy-label preparation across current local-only release behavior and future Supabase/PostHog/APNs/social/sync data flows.
- Added `scripts/savi-privacy-inventory-check.py` and wired it into `scripts/savi-preflight.sh`.
- Updated compliance, submission packet, release governance, preflight, CTO, architecture, backend, safety scan, and roadmap docs so privacy labels have a concrete source of truth before App Store Connect answers change.
- Source anchors used: Apple App Privacy Details, App Store Connect App Privacy Reference, and Manage App Privacy.

## Notification / APNs Readiness Guardrail

- Added `Docs/Backend/NotificationRunbook.md` to define the future notification permission timing, APNs backend boundary, allowed/forbidden notification content, device-token storage rules, settings requirements, analytics limits, and push launch test checklist.
- Added future notification routes and schemas to `Docs/Backend/OpenAPI/savi-social-v1.openapi.yaml` for device-token registration/deletion and notification settings.
- Added `scripts/savi-notification-readiness-check.py` and wired it into `scripts/savi-preflight.sh`.
- Updated backend, social UX, compliance, setup, release, CTO, safety scan, and preflight docs so push notifications remain future-only and privacy-safe.

## Account Deletion Readiness Guardrail

- Added `Docs/Backend/AccountDeletionRunbook.md` to define the future account deletion path, Sign in with Apple revocation expectations, Supabase cascade coverage, local-library boundaries, and account launch test checklist.
- Added `scripts/savi-account-deletion-check.py` and wired it into `scripts/savi-preflight.sh`.
- Updated compliance, API, social, security, setup, release, CTO, backend, public data-deletion, and preflight docs so account deletion stays aligned before accounts/social launch.

## Moderation Workflow Guardrail

- Added `Docs/Backend/AdminModerationWorkflow.md` to define the protected admin moderation queue, report states, public-link hide/unhide actions, audit-log requirements, and App Store social launch gate.
- Added `Docs/Backend/OpenAPI/savi-admin-v1.openapi.yaml` as a separate internal Admin Moderation API draft that must never be called from the consumer iPhone app or share extension.
- Added `scripts/savi-moderation-readiness-check.py` and wired it into `scripts/savi-preflight.sh`.
- Updated backend, desktop, CTO handoff, safety scan, and preflight docs so moderation is tracked as part of social/App Store readiness.

## Build 34 TestFlight Upload

- Bumped the iOS app and share extension to build `34`.
- Included the Home sample-clear confirmation popup in the TestFlight candidate.
- Release upload target remains `SAVI` / `com.altatecrd.savi`; `SAVI Test` remains local Debug only.
- Signed Release archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260508-232717-b34.xcarchive`.
- Upload log:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build34-20260508-232717.log`.
- Upload ended with `Uploaded SAVI` and `EXPORT SUCCEEDED`; build `1.0 (34)` is processing in App Store Connect.

## Senior Architecture Documentation

- Added a CTO/future-engineer architecture package under `Docs/Architecture/`.
- Documented SAVI's intended system shape: native iOS first, future native Android via shared backend contracts, Supabase for social/account/public-link sync, PostHog for privacy-safe analytics, optional iCloud only for private Apple backup, and Mac/admin Founder Hub separation.
- Added architecture docs for system context, client layers, backend setup, portable data model, API contract direction, analytics/business metrics, security/privacy, Android readiness, and setup/release runbooks.
- Added ADRs for:
  - native clients with a shared backend contract,
  - Supabase/PostHog/CloudKit roles,
  - Founder Hub/admin separation from the consumer app.
- Linked the new architecture package from `Docs/Backend/README.md`.

## Master Roadmap / Compliance / Social / Desktop Governance

- Added the master execution layer for future CTOs, senior engineers, and parallel Codex chats:
  - `Docs/Architecture/CTOHandoffIndex.md`,
  - `Docs/Architecture/MasterRoadmap.md`,
  - `Docs/Architecture/AppStoreComplianceMatrix.md`,
  - `Docs/Architecture/SocialV1ImplementationPlan.md`,
  - `Docs/Architecture/DesktopAndFounderHubRoadmap.md`.
- The new docs make Apple compliance, social safety, Supabase/PostHog rollout, Mac Founder Hub separation, future Mac companion work, and Android readiness living architecture concerns.
- Updated architecture README and runbooks so these docs are the front door for future implementation.

## Credential-Free Backend / Legal / Social UX Prep

- Added public-site/legal templates under `Docs/PublicSite/` for privacy, terms, support, data deletion, community guidelines, and domain/DNS planning.
- Added portable backend contracts:
  - `Docs/Backend/OpenAPI/savi-social-v1.openapi.yaml`,
  - JSON schemas for SAVI items, public links, and analytics events,
  - Supabase RLS test plan,
  - PostHog dashboard grouping spec.
- Added social mobile UX and notification basics in `Docs/Architecture/SocialMobileUXAndNotifications.md`.
- Added credential-free mock social/admin flow specs in `Docs/Architecture/MockFlows/MockSocialAndAdminFlows.md`.
- Added `scripts/savi-safety-scan.sh` to check for common secret/admin-boundary mistakes before backend/social-sensitive changes.

## Backend / Social Readiness Automation

- Added `scripts/savi-backend-readiness-check.py`.
- The readiness check validates JSON contracts, core Social V1 OpenAPI routes, Supabase table/RLS coverage, Release social gating, social safety docs, notification safety docs, and the safety scan's admin-boundary coverage.
- Updated release governance and CTO handoff docs so the readiness check is part of future backend/social-sensitive work.

## Unified Preflight Automation

- Added `scripts/savi-preflight.sh` as the one-command local gate.
- Quick preflight runs whitespace checks, safety scan, backend/social readiness, and public-site TODO reminders.
- Optional flags add iOS Release and `SAVI Mac` Debug/Release builds:
  - `--release-build`,
  - `--mac-debug-build`,
  - `--mac-release-build`,
  - `--all-builds`.
- Added `Docs/Architecture/Runbooks/PreflightAutomation.md` and linked it from release governance and CTO handoff docs.

## GitHub Preflight Workflow

- Added `.github/workflows/savi-preflight.yml`.
- The workflow runs quick SAVI preflight on pull requests and pushes to `main`/`develop`.
- Added a manual `workflow_dispatch` option, `run_builds`, to run the heavier iOS Release and `SAVI Mac` build preflight.
- Added `Docs/Architecture/Runbooks/GitHubActions.md`.
- No signing secrets, TestFlight upload, Supabase credentials, PostHog credentials, or App Store Connect keys were added.

## Backend Contract Fixtures

- Added safe valid/invalid backend payload fixtures under `Docs/Backend/Fixtures/`.
- Added `scripts/savi-contract-fixtures-check.py`, a dependency-free validator for SAVI item, public link, and analytics event payload examples.
- The fixture checker enforces important safety rules: public links must be public HTTP(S), Private Vault content cannot be public, and analytics fixtures cannot contain private note/file/screenshot/vault fields.
- Wired the fixture checker into `scripts/savi-preflight.sh` and linked it from backend and CTO handoff docs.

## App Store Submission Packet

- Added `Docs/Architecture/Runbooks/AppStoreSubmissionPacket.md` with App Store Connect field guidance, TestFlight copy, App Review notes, screenshot order, privacy-label draft, export-compliance reminder, and social/UGC release gates.
- Added `scripts/savi-appstore-readiness-check.py` to verify submission-critical docs, bundle IDs, export-compliance plist keys, Face ID copy, share extension point, App Group entitlements, and Release social gating.
- Wired the App Store readiness checker into `scripts/savi-preflight.sh`.
- The checker warns when prose is stale relative to project build settings and when public-site TODO placeholders remain, without blocking credential-free development work.

## Cross-Chat Status Reports

- Added `scripts/savi-status-report.py`, a no-account status command that summarizes current build IDs, bundle IDs, TestFlight readiness prose, Release safety gates, newest work-log entries, dirty worktree counts, public-site TODO blockers, and recommended verification commands.
- Added `Docs/Architecture/Runbooks/StatusReports.md` and linked it from the architecture README and CTO handoff index.
- Intended use: run it at the start of a new SAVI chat or before upload decisions so the team can see local-vs-TestFlight state without relying on memory.
- Refined the report so iOS app, share extension, Debug, Release, and internal Mac builds are listed separately instead of mixing iOS build `33` with Mac build `1`.

## Documentation Link Integrity

- Added `scripts/savi-docs-link-check.py`, a dependency-free checker for internal Markdown links and backticked repo paths across `AGENTS.md` and `Docs/**/*.md`.
- Wired the docs link checker into `scripts/savi-preflight.sh`.
- Linked the checker from the preflight runbook and CTO handoff index so future architecture/runbook changes stay navigable.

## Analytics Contract Guardrail

- Added `scripts/savi-analytics-contract-check.py`, a dependency-free checker that compares the app analytics event/property allowlists with the backend schema, catalog, and analytics fixtures.
- Wired the analytics contract checker into `scripts/savi-preflight.sh`.
- Aligned analytics fixtures with the app's current property names: `build_number`, `save_source_group`, and `is_public`.
- Kept `saved_item_shared_out` as a planned event only until the app actually emits it, rather than advertising it in the current backend schema.

## Supabase RLS Guardrail

- Added `scripts/savi-supabase-rls-check.py`, a dependency-free static checker for the Social V1 Supabase schema and RLS test plan.
- Wired the RLS checker into `scripts/savi-preflight.sh`.
- Hardened the Social V1 schema draft so report statuses are constrained and likes on blocked users' links are hidden in the same direction as feed/public-link visibility.
- Expanded the RLS test plan to cover blocked-like visibility and invalid report status cases.

## Social OpenAPI Contract Guardrail

- Added `DELETE /me/account` to the Social V1 OpenAPI contract and documented the account-deletion cascade/revocation expectation in the architecture API contract.
- Added request/response schemas for profiles, profile updates, public-link publishing, and reports.
- Added `scripts/savi-openapi-contract-check.py`, a dependency-free checker that verifies required Social V1 routes, bearer auth, account deletion, public-link exclusions, and OpenAPI/Supabase public item-type alignment.
- Wired the OpenAPI checker into `scripts/savi-preflight.sh`.

## Social / Analytics Backend Foundation

- Added the first safe backend foundation for the future Supabase/PostHog direction without adding live SDKs or requiring accounts.
- Added typed analytics event names and property keys in `SaviCore.swift`, plus a local Debug event log and no-op Release analytics service.
- Added an opt-in `analyticsEnabled` preference and Profile privacy analytics card; Release/TestFlight remains no-op unless a future PostHog configuration is explicitly added and approved.
- Redirected Debug social sync through a `SaviSocialBackendService` mock backend instead of CloudKit-shaped social calls. CloudKit remains reserved for optional private backup/sync later.
- Kept Release/TestFlight social hidden via `SaviReleaseGate.socialFeaturesEnabled == false`.
- Added backend docs under `Docs/Backend/`:
  - analytics event catalog,
  - PostHog dashboard plan,
  - Supabase Social V1 schema/RLS draft,
  - App Store social safety checklist,
  - safe backend config template,
  - macOS companion architecture note.
- Verification:
  - `git diff --check` passed.
  - Release generic iPhoneOS no-sign build passed.
  - Debug generic iOS Simulator build passed.

## Mac Companion Development Target

- Added a separate `SAVI Mac` macOS SwiftUI target instead of turning the iPhone app into Catalyst.
- Added `Shared/SaviCompanionCore.swift` as a small Foundation-only shared layer with companion-safe folders, items, search/filtering, mock sample data, safe backend config loading, and disabled/stubbed social/analytics clients.
- Added a first desktop shell under `SAVIMac/`:
  - sidebar navigation for Library, folders, Friends placeholder, and Settings,
  - all-saves grid/list browsing,
  - search,
  - item detail with tags/status/open-link behavior,
  - JSON import/export through user-selected panels,
  - sync/analytics status cards and local debug event log.
- Added sandboxed macOS entitlements with network client and user-selected read/write file access.
- Added a shared `SAVI Mac` scheme and kept iOS Release/TestFlight behavior unchanged.
- Supabase/PostHog remain disabled unless safe public config is supplied later. No service-role/admin keys, private files, vault content, screenshots, or raw clipboard data are sent anywhere.
- Smoke screenshot:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/mac-companion/savi-mac-smoke-20260508-181644.png`
- Verification:
  - `xcodebuild -project SAVI.xcodeproj -scheme "SAVI Mac" -configuration Debug -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build` passed.
  - `xcodebuild -project SAVI.xcodeproj -scheme "SAVI Mac" -configuration Release -destination 'platform=macOS' -derivedDataPath build/mac-companion-release-derived CODE_SIGNING_ALLOWED=NO build` passed.
  - Launched the Debug Mac app and captured the smoke screenshot above.
  - `xcodebuild -project SAVI.xcodeproj -scheme SAVI -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build` passed.
  - `xcodebuild -project SAVI.xcodeproj -scheme SAVI -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath build/mac-companion-ios-debug-derived CODE_SIGNING_ALLOWED=NO build` passed.

## Mac Founder Hub And Codex Skills

- Turned the Mac companion into a SAVI-style Founder Hub and made it the default Mac landing screen.
- Added shared founder dashboard models and a mock privacy-safe dashboard provider in `Shared/SaviCompanionCore.swift`.
- Added native Mac dashboard sections in `SAVIMac/SAVIMacRootView.swift`:
  - Pulse,
  - Share Sheet Rocket,
  - Save Engine,
  - Search Brain,
  - Rabbit Hole Radar,
  - Social Lab,
  - Reliability Room,
  - TestFlight Room,
  - Codex Workshop.
- Added Swift Charts line/bar charts, an activation funnel, retention grid, ranked public-link lists, and alert/status rows.
- Added a disabled Mac-only PostHog dashboard client path that checks Keychain status later, but makes no live calls and stores no keys in source or app bundles.
- Kept iOS Release/TestFlight unchanged: social remains hidden, founder dashboards are Mac-only, and Supabase/PostHog remain disabled unless explicitly configured later.
- Installed useful curated Codex skills into `/Users/guest1/.codex/skills/`:
  - `security-threat-model`,
  - `security-best-practices`,
  - `sentry`,
  - `playwright`,
  - `screenshot`,
  - `gh-fix-ci`,
  - `gh-address-comments`,
  - `yeet`.
- Restart Codex to load the newly installed skills into future chats.
- Smoke screenshot:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/mac-founder-hub/savi-mac-founder-hub-20260508-2121.png`
- Verification:
  - Curated skill install check confirmed all eight skills installed.
  - `git diff --check` passed before build verification.
  - `SAVI Mac` Debug macOS build passed.
  - `SAVI Mac` Release macOS build passed.
  - iOS `SAVI` Release generic iPhoneOS no-sign build passed.
  - Debug Mac app launched and the Founder Hub screenshot above was captured.
  - Secret scan found only intentional guardrail text and no PostHog personal API key, Supabase service-role key, passwords, session replay, autocapture, or private-content tracking.

## Founder Hub Mac-Only Security Separation

- Moved all Founder Hub dashboard models, mock metrics, and founder-only dashboard snapshots out of `Shared/SaviCompanionCore.swift`.
- Added `SAVIMac/SaviFounderDashboardCore.swift` as a Mac-only source file for Founder Hub dashboard types and mock data.
- Updated the Xcode project so `SaviFounderDashboardCore.swift` is included only in the `SAVI Mac` sources build phase.
- Kept the iPhone/TestFlight app compiling only the safe shared companion core: item/folder models, backend config placeholders, no-op/console analytics clients, and companion sample browsing support.
- Verification:
  - `rg "SaviFounder" Shared/SaviCompanionCore.swift` returned no matches.
  - `rg "SaviFounder|Founder Hub|Saves/User|Mock active testers|Rabbit Hole Radar|TestFlight Room|Codex Workshop|PostHog query" SAVI SAVIShareExtension Shared` returned no matches.
  - Xcode project search shows `SaviFounderDashboardCore.swift` only in the `SAVI Mac` source list, not in the `SAVI` iPhone or share extension source lists.
  - `git diff --check` passed.
  - `SAVI Mac` Debug macOS build passed.
  - `SAVI Mac` Release macOS build passed.
  - iOS `SAVI` Release generic iPhoneOS no-sign build passed.
  - Secret scan found only intentional guardrail text and no actual PostHog personal API key, Supabase service-role key, passwords, session replay, autocapture, or private-content tracking.
