# 2026-05-07

## TestFlight Build 14 - Share Metadata Hotfix

- Fixed Release/TestFlight metadata regression where shared links could stay
  generic after save because Release had link metadata repair disabled.
- Re-enabled Release `LinkPresentation` metadata use on supported iOS versions
  and re-enabled stale metadata repair after app launch/foreground.
- Added device-aware metadata limits through `SaviPerformancePolicy`:
  - modern devices can repair more metadata concurrently;
  - iPhone 11/legacy devices repair only a tiny batch at launch/foreground.
- Prevented stale metadata repair from chaining through the whole library on
  older phones. Only share-import/deeplink/pasteboard saves can trigger one
  follow-up repair after their own metadata completes.
- Improved the share extension metadata window:
  - increased remote metadata timeout from `2.2s` to `3.8s`;
  - increased JSON/HTML request timeout from `2s` to `3.5s`;
  - enabled LinkPresentation fallback for the share extension on iOS 18+.
- Sanitized metadata logs so data-URL thumbnails are reported as `data-url`
  instead of dumping giant base64 strings.
- Bumped `CURRENT_PROJECT_VERSION` from `13` to `14`.
- Verified `git diff --check` passes before the build.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Built and launched Release on local iPhone 11 simulator
  `SAVI Crash QA iPhone 11` (`7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`):
  launch succeeded, logs showed `perfTier=legacy`, one stale metadata repair
  was scheduled, and no repair chain occurred.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260507-013124-b14.xcarchive`.
- Uploaded version `1.0` build `14` to App Store Connect with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build14-metadata-hotfix-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build13-metadata-hotfix-iphone11-launch.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build14-metadata-hotfix.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build14.log`

## TestFlight Build 17 - Pilot Sync, Onboarding, and Share Extension Polish

- Bumped the SAVI app and share extension to build `17`.
- Tightened first-run onboarding so new installs open on the value-first
  “Save it now. Find it later.” card before the Share Sheet setup card.
- Polished the share extension tag editor:
  - smaller single-line smart tag pills;
  - visible `Send` tag button;
  - keyboard return commits tags without trapping the keyboard;
  - accessory toolbar supports adding a tag or dismissing the keyboard.
- Hardened simulator sync with explicit simulator IDs, clean install backups,
  stale bundle removal, and app/share-extension build reporting.
- Clean-installed build `17` on all booted simulators:
  - iPhone Air `0ACA1911-2960-4ECD-B178-B8A678360814`;
  - iPhone 17 `CC9E6699-BC90-42D3-AA84-C82F207B1B25`;
  - SAVI Crash QA iPhone 11 `7CEA2B7D-F862-469E-85EE-2F2DCEDCF295`.
- Confirmed every simulator has:
  - `com.altatecrd.savi` build `17`;
  - `com.altatecrd.savi.ShareExtension` build `17`;
  - `com.altatecrd.savi.personaldebug` build `17`;
  - `com.altatecrd.savi.personaldebug.ShareExtension` build `17`;
  - no stale `com.savi.app` install.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- Created signed archive:
  `/Users/guest1/Documents/SAVI-iOS/build/qa/archives/SAVI-20260507-122829-b17.xcarchive`.
- Uploaded version `1.0` build `17` to App Store Connect/TestFlight with
  `xcodebuild -exportArchive`; upload succeeded and App Store Connect began
  processing the package.
- Logs:
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-build17-release-iphoneos-nosign.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-archive-build17.log`
  - `/Users/guest1/Documents/SAVI-iOS/build/qa/logs/SAVI-export-upload-build17.log`

## Simulator Workflow Optimization

- Added `scripts/savi-fast-dev-sim.sh` for everyday SAVI iteration.
- The fast loop builds and launches only Debug `SAVI Test` on one simulator,
  defaulting to iPhone 17.
- Added `--reinstall` for one-simulator Share Extension refreshes without
  rebuilding Release or touching TestFlight.
- Documented that the full simulator matrix and Release/TestFlight upload path
  are reserved for explicit release gates, not every small feature pass.

## Build 18 Candidate Cleanup

- Bumped the SAVI app and share extension to build `18` for the next candidate.
- Changed app and share extension `CFBundleVersion` to read
  `$(CURRENT_PROJECT_VERSION)` so future build bumps apply everywhere.
- Cleaned the combined dirty work from parallel chats into one source-tree
  direction before any TestFlight upload.
- Removed unused onboarding/share setup demo code and the unused generated
  `onboarding-search-real` asset.
- Kept the real share-sheet practice flow: the practice image now counts as the
  same setup example when it returns through the actual Share Extension.
- Clean-installed both `SAVI` and `SAVI Test` on the primary iPhone 17 Pro
  simulator; app and share extension channels all report `1.0 (18)`.
- Verified `git diff --check` passes.
- Verified Release generic iPhoneOS no-sign build succeeds.
- No TestFlight upload was performed in this cleanup pass.
