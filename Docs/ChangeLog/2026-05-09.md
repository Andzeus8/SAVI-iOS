# 2026-05-09

## Home Sample Clear Button De-Emphasis

- Replaced the prominent Home sample-library explanation card with one quieter
  `Clear sample saves` button.
- Muted the destructive styling to soft red text/stroke on a neutral surface
  instead of a full red button, removing the large chartreuse icon, gradient
  card, and shadow.
- Kept the confirmation alert and sample-clear logic unchanged.
- `git diff --check` passed.
- Debug iPhone simulator build passed and the latest app was relaunched on the
  booted iPhone 17 Pro simulator.

## Share Sheet Note And Tag Cleanup

- Grouped the Share Sheet suggested tags, custom tag input, and optional note
  controls into one soft details area inside the preview card.
- Added clearer `Tags` and `Note` subheaders, removed the duplicate tiny inline
  tags label, and made `Add note` / `Clear` feel like aligned pill controls
  instead of floating text.
- Tightened custom-tag input spacing and changed the placeholder to
  `Add custom tag`.
- Kept share persistence, metadata enrichment, folder selection, signing,
  upload, backend, and build-number behavior unchanged.
- `git diff --check` passed.
- Debug iPhone simulator build passed, including `SAVIShareExtension`.

## GitHub Source Sync

- Staged the full coordinated worktree so GitHub matches the latest build `35`
  source state, including iOS/share-extension polish, architecture/backend
  docs, Mac companion/founder hub scaffolding, public-site drafts, and
  preflight scripts.
- Ran `git diff --cached --check` after trimming extra EOF blank lines in new
  docs/scripts.
- Ran `scripts/savi-safety-scan.sh`; it passed with no likely app/admin
  secrets found outside docs/build/git.
- Pushed commit `c336989` to GitHub branch
  `origin/codex/savi-build17-pilot-sync`.

## Build 35 TestFlight Upload

- Bumped the iOS app and share-extension build settings to `35`.
- Restored app and share-extension `CFBundleVersion` to
  `$(CURRENT_PROJECT_VERSION)` so future build bumps propagate into archived
  bundles.
- Signed and uploaded `SAVI 1.0 (35)` to App Store Connect/TestFlight; upload
  completed with `Uploaded SAVI` and `EXPORT SUCCEEDED`, and the build is now
  processing in App Store Connect.
- Verified the corrected archive reports bundle version `35` for both
  `com.altatecrd.savi` and `com.altatecrd.savi.ShareExtension`.

## Home Sample Clear Action Visibility

- Reworked the Home sample-library notice into a clearer control card with
  headline/body copy and a full-width red `Clear sample saves` button.
- Improved light/dark readability with stronger button contrast, border,
  shadow, and copy that reassures users their personal saves stay untouched.
- Kept the card above the Home widgets/recent-saves area and preserved the
  existing confirmation alert and sample-clear logic.

## Simulator Refresh For Local UI QA

- Reinstalled and launched the latest local Debug `SAVI` build on the booted
  `iPhone 17 Pro` simulator through XcodeBuildMCP.
- Verified launch with a simulator screenshot; no signing, upload,
  build-number, App Store Connect, backend, account, or source behavior changed.

## Share Extension Suggested-Folder Clarity

- Updated the Share Sheet save UI so the folder summary shows the actual
  selected or suggested folder name instead of the generic `Where to save`
  label.
- Made auto mode read as `SAVI Brain`, with clearer hint copy for metadata,
  rules, Apple Intelligence, and manual folder choices.
- Changed the folder summary icon/toggle treatment so users can tell whether
  SAVI is auto-picking or they have chosen a folder, while keeping the existing
  two-column folder grid and tag flow unchanged.
- No share-save persistence, metadata enrichment, backend, signing, upload,
  account, or build-number behavior changed.

## First-Run Share Setup UX Polish

- Rewrote the first-run onboarding copy so SAVI immediately explains its core
  value: scattered links, screenshots, PDFs, videos, and Airbnb Wi-Fi codes
  saved calmly in one place.
- Updated the smart-sorting onboarding page to `SAVI Brain`, with clearer copy
  that SAVI suggests the title, folder, and tags before saving.
- Tightened the Share Sheet setup flow around the essential one-minute action:
  pin SAVI once, share the practice image, confirm it lands in the library, and
  then save from Safari, Photos, Files, YouTube, and other apps faster.
- Kept this as a UI/UX copy/layout pass only: no share-extension logic, assets,
  signing, upload, accounts, backend, or build-number behavior changed.

## App Store Export Compliance Packet

- Added `Docs/Architecture/Runbooks/AppStoreExportCompliance.md` with the
  current iOS app/share-extension export-compliance posture:
  `ITSAppUsesNonExemptEncryption = NO`, Apple/system services plus HTTPS/TLS,
  no expected custom/proprietary/bundled third-party crypto, founder/legal
  final-answer reminder, and triggers for re-review.
- Added `scripts/savi-appstore-export-compliance-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated App Store submission, App Store metadata, compliance matrix,
  production/TestFlight readiness, release governance, status reports, CTO
  handoff, architecture README, safety scan, metadata/readiness checkers, and
  status-report command recommendations.

## App Store Age Rating Answer Packet

- Added `Docs/Architecture/Runbooks/AppStoreAgeRating.md` with the current
  private-save-first age-rating posture: no gambling, no contests, no
  unrestricted web browser, no live public social/UGC, no DMs/comments, no
  public user-generated PDFs/screenshots/files/vault content, neutral health
  research framing, Kids-category caution, and future re-rating triggers.
- Added `scripts/savi-appstore-age-rating-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated App Store Connect metadata, submission packet, compliance matrix,
  release governance, status reports, CTO handoff, architecture README, safety
  scan, metadata/readiness checkers, and status-report command recommendations.

## Archive Export And Restore QA Guardrail

- Added `Docs/Architecture/Runbooks/ArchiveExportRestoreQA.md` for Profile
  backup, full ZIP archive export, compact JSON backup, cancel states, invalid
  file handling, fresh-install restore, Private Vault behavior, sample-library
  separation, and the rule that restore never publishes or uploads anything in
  Release.
- Added `scripts/savi-archive-restore-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated production readiness, TestFlight readiness, App Store submission,
  release governance, status reports, CTO handoff, architecture README, safety
  scan, and status-report command recommendations.

## App Store Privacy Labels Answer Packet

- Added `Docs/Architecture/Runbooks/AppStorePrivacyLabels.md` with the current
  private-save-first privacy-label draft, conservative support-disclosure
  alternative, remote metadata notes, and future Supabase/PostHog/APNs/social
  update triggers.
- Added `scripts/savi-appstore-privacy-labels-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated the App Store privacy worksheet, privacy data inventory, compliance
  matrix, App Store submission packet, preflight docs, status reports, CTO
  handoff, architecture README, safety scan, and status-report command
  recommendations.

## Share Extension Real-Device QA Guardrail

- Added `Docs/Architecture/Runbooks/ShareExtensionRealDeviceQA.md` for the
  real-device Share Sheet save matrix: Safari, YouTube, TikTok/Instagram/X,
  Photos screenshots/images, Files PDFs/generic files, plain text, offline URL
  fallback, metadata resilience, app-group import checks, and privacy-safe
  tester notes.
- Added `scripts/savi-share-extension-qa-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated production readiness, TestFlight readiness, App Store submission,
  release governance, status reports, CTO handoff, architecture README, safety
  scan, and status-report command recommendations.

## Crash And Performance Triage Guardrail

- Added `Docs/Architecture/Runbooks/CrashAndPerformanceTriage.md` for
  iPhone 11/iOS 17-style launch crashes, freezes, slow Home/Search/Explore
  scrolling, thumbnail jank, share extension failures, TestFlight feedback
  intake, privacy-safe diagnostic notes, and fix discipline.
- Added `scripts/savi-crash-performance-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated production readiness, TestFlight operations, status reports, preflight
  docs, CTO handoff, architecture README, safety scan, and status-report command
  recommendations.

## TestFlight Operations Guardrail

- Added `Docs/Architecture/Runbooks/TestFlightOperations.md` with current build
  `1.0 (34)`, internal group `SAVI Internal`, tester emails to verify, build
  assignment steps, tester install instructions, old-build troubleshooting, and
  source anchors to Apple's TestFlight docs.
- Added `scripts/savi-testflight-ops-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Updated TestFlight readiness, App Store submission packet, preflight docs,
  CTO handoff, architecture README, safety scan, and status report command
  recommendations.

## App Store Metadata Packet Guardrail

- Added `Docs/Architecture/Runbooks/AppStoreConnectMetadata.md` as a
  copy-paste App Store Connect metadata packet for app record fields, product
  page copy, keywords, TestFlight beta description, What to Test, App Review
  notes, screenshot order, age-rating notes, and final human replacements.
- Added `scripts/savi-appstore-metadata-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Fixed stale `Docs/TestFlightReadiness.md` references to build `27` and the
  old "Build 4" heading; the readiness doc now points to latest uploaded
  build `1.0 (34)`.
- Updated the App Store submission packet, CTO handoff, architecture README,
  preflight docs, and safety scan front doors.

## Public-Site Static Export

- Added `scripts/savi-public-site-build.py`, a dependency-free exporter that
  converts the public-site Markdown pages into static HTML under
  `build/public-site/`.
- Added `scripts/savi-public-site-check.py` and wired it into
  `scripts/savi-preflight.sh`.
- Added `Docs/PublicSite/StaticSitePublishing.md` with deployment shape,
  generated page list, and secret/admin content that must never be published.
- Updated public-site README, setup checklist, preflight docs, and App Store
  submission packet to reference the static export flow.

## Public-Site Draft Cleanup

- Replaced remaining `TODO` placeholders in the public-site privacy, terms,
  support, and data-deletion draft pages with the current documented beta
  support/privacy contact: `1080solutionsA@gmail.com`.
- Updated public-site README language from placeholder pages to
  publication-ready drafts that still require final domain/company/legal review.
- Updated preflight documentation so public-site TODO warnings are no longer
  expected for normal release prep.
