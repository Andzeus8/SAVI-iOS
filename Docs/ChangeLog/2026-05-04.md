# 2026-05-04

## Build Channel Lock

- Confirmed the project should stay on two installed app channels from one
  source tree:
  - Release installs `SAVI` as `com.savi.app`.
  - Debug installs `SAVI Test` as `com.altatecrd.savi.personaldebug`.
- Kept Release/TestFlight behavior beta-safe: Social Beta off and debug tools
  off; later in the day we enabled a removable sample library for beta onboarding.
- Kept Debug behavior developer-friendly: Social Beta on and demo/debug tools
  available.
- Decided not to create a third `SAVI Pilot` app unless a separate internal QA
  mirror is needed later.
- Updated the TestFlight and production-readiness docs with the channel rule so
  future work does not confuse `SAVI` with `SAVI Test`.
- Updated share-extension fallback copy so Debug says `SAVI Test` and Release
  says `SAVI`.

## TestFlight Hardening Pass

- Kept `SAVI` Release as the first external TestFlight channel and `SAVI Test`
  Debug as the internal development channel.
- Added `ITSAppUsesNonExemptEncryption = NO` to the app and share-extension
  Info.plists to match the current standard Apple/HTTPS/CloudKit-only
  encryption posture; final App Store Connect export compliance still requires
  owner confirmation.
- Changed the first external beta target posture to iPhone-only and portrait-only
  so TestFlight matches the screens that have actually been designed and QA'd.
- Updated `generate_project.py` so regenerating the Xcode project preserves the
  current Team ID, build number, iPhone-only target family, and sample-library
  build setting.
- Updated TestFlight/production readiness docs with the applied hardening and
  remaining upload checklist.

## Build / Simulator QA

- Build/install helper used:
  `scripts/savi-install-both-sim.sh --no-launch`
- Refreshed simulator channels:
  - `SAVI Test` / `com.altatecrd.savi.personaldebug` / Debug / Social visible.
  - `SAVI` / `com.savi.app` / Release / Social hidden.
- Verified both installed simulator apps are build `2`, including both share
  extensions.
- Ran Release iPhoneOS non-signing build check:
  `xcodebuild -project SAVI.xcodeproj -scheme SAVI -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build`
- Result: build succeeded.

## Launch Sample Library

- Updated default launch folder names/order while keeping stable folder IDs:
  `AI & Work`, `Memes & Laughs`, `Places & Trips`, `Notes & Clips`,
  `Research & PDFs`, `Health`, `Science Finds`, `Rabbit Holes`, and
  `Everything Else`.
- Previously expanded the seed sample library from 10 saves to 65 removable
  sample saves, five per launch folder, with varied dates, tags, types, sources,
  fake private vault examples, and deterministic sample thumbnails.
- Added a runtime `SAVISampleLibraryEnabled` flag so TestFlight can include the
  sample library while a final App Store build can turn it off cleanly later.
- Added Home/Profile sample-library copy and a visible `Clear sample saves`
  action that removes only `demo == true` sample items.
- Updated folder intelligence defaults, guidance, Apple Intelligence prompt
  copy, and share-extension fallback folder presets to use the new folder names.
- Updated TestFlight readiness docs to explain that Social stays hidden while a
  removable sample library is included for beta onboarding.

## Folder Readability Polish

- Created rollback archive:
  `Docs/Design/FolderReadabilityArchive/2026-05-04-before-folder-readability/`
- Kept the current colorful folder cards, but made folder names easier to scan
  with a shared title component, calmer title weight, stricter two-line breaks,
  fixed title-block alignment, and slightly smaller icon badges.
- Applied the same folder title treatment to Home folder tiles and the full
  Folders grid so one-line and two-line names feel intentionally aligned.

## Sample Library Reseed Safety

- Fixed main `SAVI` not showing the launch sample library after sample content
  had been cleared on an empty simulator library.
- Empty, sample-suppressed libraries now restore the current sample library once
  when `SAVISampleLibraryEnabled` is on and the stored seed version is older.
- `Restore sample library` now preserves personal saves by adding sample saves
  alongside user content instead of replacing the library.
- Added a Home empty-state `Load sample library` action and renamed the Profile
  recovery action to the same clearer copy.

## Launch Sample Library V4

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v4-sample-library/`.
- Added the new first folder `Life Admin` (`f-life-admin`) for practical,
  non-secret admin examples like door codes, Wi-Fi notes, contract templates,
  sample insurance cards, and recovery codes.
- Replaced the generic 65-save sample catalog with a 70-save utility-first
  launch catalog: five examples in each visible launch folder, with the Home
  timeline opening on `Airbnb door code + Wi-Fi`, fake sample ID/insurance
  documents, reusable admin files, evidence-safe health/research links, places,
  AI/work notes, memes, and science saves.
- Generated local watermarked `SAMPLE` document thumbnails for fake documents
  instead of bundling third-party ID/passport/insurance imagery.
- Kept health content neutral and evidence-oriented with CDC, PubMed/PMC, and
  ClinicalTrials.gov links; no cleanse/cure claims.
- Bumped native seed version to `7` so older sample libraries refresh safely,
  while preserving personal saves and repairing the new first-launch folder
  order.
- Updated folder intelligence, Apple Intelligence guidance, share-extension
  presets, share smart tags, and folder visual styling for `Life Admin`.
- Made demo-only Home folder strips use the deliberate launch order instead of
  recency, so first-run users see `Life Admin`, `Health`, `Watch / Read Later`,
  `AI & Work`, and `Places & Trips` up front.
- Updated generic-folder repair so it does not reclassify curated demo saves
  out of `Everything Else` during first launch.

## Launch Sample Library V5

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v5-sample-library/`.
- Kept the sample catalog at 70 saves, but reordered the first-run timeline into
  a more fluent mix of utility, health/research, travel, AI prompt, documents,
  maps, memes, and science saves.
- Added `Hotel booking confirmation` to `Life Admin` and upgraded the AI prompt
  sample to `Prompt: turn chaos into a plan` with stronger `ai-prompt`,
  `workflow`, and `template` tags.
- Replaced repeated generic document thumbnails with distinct generated
  watermarked mockups for Airbnb access, hotel booking, driver license,
  insurance card, contract template, recovery code, AI prompt, vault items,
  climate PDF, and market spreadsheet samples.
- Bumped native seed version to `8` so existing sample libraries can refresh to
  the V5 catalog while preserving personal saves.
- Built and installed both simulator channels, cloned main data into `SAVI Test`,
  and captured QA screenshot:
  `/Users/guest1/Desktop/SAVI_QA/savi_v5_sample_home_2026-05-04.png`.

## Folder Editor Cover Image Fix

- Created rollback archive:
  `Docs/Design/FolderEditorArchive/2026-05-04-before-cover-image-editor/`.
- Fixed Folder editor artwork semantics so choosing a photo/web image now
  defaults to a full Folder cover background with readability overlay, while
  the Folder symbol remains a separate icon control.
- Selecting a new symbol no longer deletes an existing cover image.
- Updated the preview card to render the same cover-background treatment used
  by Home and Folders, instead of showing image artwork only as an icon.
- Changed unconfigured web image search from a tappable error path into a
  disabled/clear setup state. `Find image` still requires
  `SAVI_IMAGE_SEARCH_BASE_URL` and the Pexels proxy to be configured.

## Image Search Proxy Dev Fallback

- Created rollback archive:
  `Docs/Design/FolderEditorArchive/2026-05-04-before-image-proxy-dev-fallback/`.
- Made the local image-search proxy usable for simulator folder-cover QA even
  when `PEXELS_API_KEY` is not configured.
- Added a `SAVI Demo` fallback provider that returns deterministic development
  image results and lets `/image-download` convert selected demo images into
  normal JPEG data URLs for the app.
- Kept production behavior explicit: set `SAVI_IMAGE_PROXY_DEMO_FALLBACK=0` to
  disable demo images and require a real Pexels API key.
- Updated proxy setup docs to explain local simulator usage with
  `SAVI_IMAGE_SEARCH_BASE_URL=http://127.0.0.1:8787`.
- Verified `/health`, `/image-search`, and `/image-download`; launched
  `SAVI Test` with the local proxy URL so the Folder editor can enable
  `Find image` during simulator QA.

## Sample Library V6 And Thumbnail Preview

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v6-sample-thumbnails-and-preview/`.
- Made first-run utility samples more visually mixed: the Airbnb access save now
  looks like a stay/access card, and the hotel booking save now looks like a
  real confirmation card with dates, confirmation number, QR-style block, and
  check-in details.
- Strengthened AI prompt samples with actual reusable prompt text for turning
  chaos into a plan, cross-checking blind spots, and finding recurring talents.
- Added distinct AI prompt thumbnails for the new prompt examples instead of
  leaving them as generic note cards.
- Bumped the native sample seed version to `9` so empty/sample libraries can
  refresh to the new V6 catalog while preserving personal saves.
- Fixed item detail dead taps for sample notes/files that only have a thumbnail:
  tapping the preview/title now opens a thumbnail image viewer when no URL or
  saved asset exists, and the action panel shows `View Image` for those cases.

## Sample Library V7 Visual Mix

- Reused the V6 rollback archive and expanded its forward/revert patches to
  cover the final V7 sample-visual pass.
- Added custom note-style thumbnails for more text saves so launch content no
  longer falls back to repeated generic placeholders:
  dinner shopping list, gift ideas, packing list, reusable email, quote, quick
  clipboard clip, recovery protocol, and protein snack list.
- Added a custom video-style thumbnail for the `Goyo goyo with lyrics` sample
  and the documentary save so the demo library has a better media/document/note
  rhythm.
- Bumped the native sample seed version to `10` so empty/sample libraries can
  refresh into the V7 catalog while preserving personal saves.

## Sample Library V8 First-Scroll Visual Polish

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v8-first-scroll-visual-polish/`.
- Reordered the launch sample timeline so the first scroll shows a stronger mix:
  Airbnb access, Jennifer's ramen recommendation, fasting/autophagy research,
  AI agent workflow, sugar/cancer myth-check notes, sample ID, hotel booking,
  recovery code, a meme/video save, and NASA science.
- Added richer generated full-bleed sample thumbnails for stay access, food
  recommendations, health research, AI agent guidance, and myth-check saves.
- Kept health samples evidence-oriented and neutral, using source-backed titles
  such as `Fasting and autophagy: what research says` and
  `Sugar and cancer myths: evidence notes`.
- Bumped the native sample seed version to `11`; main `SAVI` refreshed to
  71 sample saves and was cloned into `SAVI Test` for simulator parity.
- Built and installed both simulator channels, then captured QA screenshot:
  `/Users/guest1/Desktop/SAVI_QA/savi_v8_sample_home_2026-05-04.png`.

## Sample Library V9 Live Metadata Mix

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v9-live-metadata-mix/`.
- Added a per-item sample metadata policy so real launch links can opt into a
  one-time live metadata refresh while fake utility/admin samples preserve
  their generated thumbnails.
- Added a real YouTube sample near the top:
  `Inside the Mind of a Master Procrastinator | Tim Urban | TED`, using
  YouTube metadata and thumbnail.
- Converted top real links for PubMed, Anthropic, NCI/Cancer, and NASA to use
  actual metadata/image URLs instead of SAVI-generated editorial art.
- Bumped the native sample seed version to `12`; main `SAVI` refreshed to
  72 sample saves and was cloned into `SAVI Test` for simulator parity.
- Built and installed both simulator channels, then captured QA screenshot:
  `/Users/guest1/Desktop/SAVI_QA/savi_v9_live_metadata_home_2026-05-04.png`.

## Sample Library V10 Home Storyboard Order

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v10-home-storyboard-order/`.
- Reordered the first Home timeline as a utility-first storyboard: Airbnb access
  note, TED/YouTube video, Jennifer's ramen recommendation, fake driver license,
  PubMed fasting research, AI planning prompt, hotel booking, Mom's lasagna
  sauce audio note, meme/video, NCI cancer myths, fake insurance card, NASA
  Webb, contract template, parasite/gut-health notes, recipe link, recovery
  code, AI blind-spot prompt, map/place save, climate PDF, gift note, and a
  private vault passport sample.
- Replaced Jennifer's recommendation thumbnail with a distinct top-down ramen
  table image plus friend/avatar and map-pin cues so it no longer resembles the
  Airbnb access card.
- Added an audio sample, `Mom's lasagna sauce voice note`, to demonstrate that
  SAVI can save voice-memo-style recipe instructions alongside links, docs, and
  notes.
- Kept real links metadata-first for YouTube/TED, PubMed, NCI, CDC,
  ClinicalTrials, NASA, Anthropic, Apple, and BBC Good Food where available,
  while fake/admin samples keep generated sample-safe thumbnails.
- Bumped the native sample seed version to `13` so empty/sample libraries can
  refresh to the V10 storyboard while preserving personal saves.
- Built and installed both simulator channels; main `SAVI` refreshed to 74
  sample saves and was cloned into `SAVI Test` for simulator parity.
- Captured QA screenshots:
  `/Users/guest1/Desktop/SAVI_QA/savi_v10_storyboard_home_2026-05-04.png`
  and
  `/Users/guest1/Desktop/SAVI_QA/savi_v10_storyboard_home_mid_2026-05-04.png`.

## Sample Library V11 Utility-First Home Order

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v11-utility-first-home-order/`.
- Reordered the first Home timeline so the launch story starts with immediate
  utility: Mom's lasagna sauce voice note, Airbnb access, fasting research,
  Jennifer's ramen recommendation, fake driver license, fake insurance card,
  mebendazole case-report research, TED/YouTube, AI prompt, hotel booking, NCI
  cancer myths, meme/video, NASA Webb, contract template, parasite/gut-health
  notes, recipe link, recovery code, AI blind-spot prompt, map save, climate
  PDF, gift note, and a private vault passport sample.
- Replaced the audio sample thumbnail with a lasagna-forward generated image
  plus a small waveform badge while keeping the item file-backed with
  `audio/mp4` metadata for Audio search/filter behavior.
- Reworked the mebendazole sample into a neutral PubMed case-report save:
  `Tumor remission case report: mebendazole research`, stored in `Health` with
  research/case-report/doctor-question tags instead of treatment claims.
- Bumped the native sample seed version to `14` so empty/sample libraries can
  refresh to the V11 storyboard while preserving personal saves.
- Built and installed both simulator channels; main `SAVI` refreshed to 74
  sample saves at seed version `14` and was cloned into `SAVI Test`.
- Captured QA screenshots:
  `/Users/guest1/Desktop/SAVI_QA/savi_v11_utility_home_2026-05-04.png`
  and
  `/Users/guest1/Desktop/SAVI_QA/savi_v11_utility_home_mid_2026-05-04.png`.

## Sample Library V12 Thumbnail And Home Order Polish

- Created rollback archive:
  `Docs/Design/SampleLibraryArchive/2026-05-04-before-v12-thumbnail-order-polish/`.
- Added bundled sample thumbnail sources for lasagna, door/access, and hotel
  booking saves, recorded in `Docs/Design/SampleLibrarySources.md`.
- Reworked the first Home sample order so it opens with Mom's lasagna voice
  note, Airbnb access, `Mebendazole and cancer remission?`, Jennifer's ramen
  recommendation, driver's license, insurance, TED/YouTube, AI prompt, hotel
  booking, NCI myths, intermittent fasting research, and Goyo.
- Upgraded the Mom voice note, Airbnb access, and hotel booking thumbnails to
  photo-backed full-bleed cards with small, useful overlays.
- Replaced the driver license and insurance thumbnails with fuller fake
  document cards using invented data and visible sample treatment instead of
  generic note art.
- Added a dedicated mebendazole research thumbnail and kept the copy medically
  neutral as a PubMed case-report save for doctor questions.
- Tightened the main AI prompt copy to `Prompt: turn chaos into next actions`
  with a stronger reusable prompt body.
- Bumped native sample seed version to `15`; main `SAVI` refreshed to 74 sample
  saves and was cloned into `SAVI Test`.
- Built and installed both simulator channels:
  `com.savi.app` and `com.altatecrd.savi.personaldebug`.
- Captured QA screenshots:
  `/Users/guest1/Desktop/SAVI_QA/savi_v12_thumbnail_home_2026-05-04.jpg`
  and
  `/Users/guest1/Desktop/SAVI_QA/savi_v12_thumbnail_home_mid_2026-05-04.jpg`.

## Social Teaser And Profile Cleanup

- Created rollback archive:
  `Docs/Design/SocialTeaserProfileArchive/2026-05-04-before-social-teaser-profile-cleanup/`.
- Kept `SAVI` Release/TestFlight social-safe: Explore shows only `All` and
  `Mine`, with a subtle `Friends are coming` Beta teaser instead of a real
  Friends tab, add-friend input, public profile editing, or public publishing.
- Kept `SAVI Test` Debug social-enabled for internal development: Friends scope,
  friend actions, and the Social Beta profile card remain visible.
- Reworked Release Profile into a cleaner stack: compact `SAVI Beta` card,
  Backups, Private Vault, Appearance, Guide, Account/iCloud, Library tools, and
  a small `Coming soon` Social Beta note.
- Softened the disabled social sheet copy to explain that Social is coming
  rather than presenting a broken feature.
- Built and installed both simulator channels:
  `com.savi.app` and `com.altatecrd.savi.personaldebug`.
- Captured QA screenshots:
  `/Users/guest1/Desktop/SAVI_QA/savi_social_teaser_explore_release_2026-05-04.jpg`,
  `/Users/guest1/Desktop/SAVI_QA/savi_social_teaser_explore_footer_release_2026-05-04.jpg`,
  `/Users/guest1/Desktop/SAVI_QA/savi_profile_cleanup_release_2026-05-04.jpg`,
  `/Users/guest1/Desktop/SAVI_QA/savi_test_social_explore_debug_2026-05-04.jpg`,
  and
  `/Users/guest1/Desktop/SAVI_QA/savi_test_social_profile_debug_2026-05-04.jpg`.

## Release iPhone Size QA

- Ran the Release `SAVI` / `com.savi.app` production UI screenshot matrix for
  representative iPhone portrait sizes in light and dark mode:
  iPhone SE, iPhone 17, iPhone Air, and iPhone 17 Pro Max.
- Updated the simulator-only QA launch overrides so Release simulator builds can
  bypass onboarding and deep-launch the requested tab without affecting
  TestFlight/device Release builds.
- Increased the QA script cold-launch wait budget so fresh Release simulator
  installs with the sample library do not get marked as launch failures.
- Fixed a small-screen Folders polish issue where the primary action clipped as
  `New fol...` on iPhone SE; the button now falls back cleanly to `New` on
  narrow widths.
- Final screenshot matrix: `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/`
- Recaptured the iPhone Air light-mode Folders screenshot with a longer render
  wait after one blank early capture; the final file in the matrix folder is the
  reviewed replacement.
- Contact sheets reviewed:
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-all-light.png`,
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-all-dark.png`,
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-home.png`,
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-search.png`,
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-explore.png`,
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-folders.png`,
  and
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-222254/contact-profile.png`.
- Result: all 40 expected screenshots were captured; Home, Search, Explore,
  Folders, and Profile are usable on the smallest and largest tested iPhone
  sizes, with no important controls hidden behind the bottom nav in the first
  viewport.

## TestFlight Final Prep Checkpoint

- Re-ran the Release `SAVI` / `com.savi.app` TestFlight gates after the final
  sample library, dark-mode, profile, and social-gating work.
- Verified build/channel settings:
  Release resolves to `com.savi.app`, display name `SAVI`, build `2`,
  `TARGETED_DEVICE_FAMILY = 1`, `SAVISampleLibraryEnabled = YES`, and
  `SAVI/SAVI.entitlements`.
- Re-linted app/share Info.plists and privacy manifests:
  `SAVI/Info.plist`, `SAVIShareExtension/Info.plist`,
  `SAVI/PrivacyInfo.xcprivacy`, and
  `SAVIShareExtension/PrivacyInfo.xcprivacy`.
- Re-ran Release iPhoneOS non-signing build:
  `xcodebuild -project SAVI.xcodeproj -scheme SAVI -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build`
  and it succeeded.
- Hardened `Tools/savi-production-ui-qa.sh` so simulator lookup uses exact
  names, the script waits for `simctl bootstatus -b`, and any install/launch
  failure exits non-zero instead of quietly producing an incomplete matrix.
- Cleaned generated QA/DerivedData artifacts when the disk filled during the
  release matrix; no source files were removed.
- Final clean screenshot matrix:
  `/Users/guest1/Documents/SAVI-iOS/build/production-ui-qa/20260504-234023/`.
- Result: all 40 expected Release screenshots were captured with no failures.
- Created local release checkpoint tag `testflight-build-2-2026-05-04`.
- Attempted a signed Release archive for `SAVI`; the code reached provisioning
  input collection but Xcode could not continue because there is no local Apple
  account for Team `ZBY2F3W785` and no provisioning profiles for
  `com.savi.app` or `com.savi.app.ShareExtension`.
- Remaining manual/TestFlight blockers: add/refresh the Apple Developer account
  in Xcode, create/download production provisioning for the app and share
  extension, complete App Store Connect metadata, confirm export compliance
  legally, run real-device share extension QA, and upload the archive.
