diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/Docs/ChangeLog/2026-05-02.md /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/Docs/ChangeLog/2026-05-02.md
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/Docs/ChangeLog/2026-05-02.md	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/Docs/ChangeLog/2026-05-02.md	2026-05-02 18:06:25
@@ -152,6 +152,23 @@
   `/Users/guest1/Desktop/SAVI_QA/search_after_minimalist_filtered.png`,
   `/Users/guest1/Desktop/SAVI_QA/search_refine_after_minimalist_optimized.jpg`,
   `/Users/guest1/Desktop/SAVI_QA/item_detail_after_minimalist.jpg`
+- Archived the pre-pass folder implementation before polishing the folder
+  palette and adding optional folder image backgrounds:
+  `/Users/guest1/Documents/SAVI-iOS/Docs/Design/FolderPolishArchive/2026-05-02-before-folder-palette-image-background/`
+- Softened the light-mode folder palette through the shared
+  `SaviFolderVisualStyle` path so Home folder tiles, All Folders grid tiles, and
+  folder list rows keep their playful color identity while blending more calmly
+  with the compact editorial Home screen.
+- Reduced folder icon badge stroke/shadow intensity and icon foreground
+  opacity so icons remain useful without competing as hard with folder names.
+- Added a persisted `uses_image_background` folder setting and editor toggle so
+  a chosen/searched folder image can become the whole folder card background,
+  with SAVI applying a readability overlay and falling back to the color card if
+  no image exists.
+- Folder polish QA screenshots:
+  `/Users/guest1/Desktop/SAVI_QA/home_after_folder_palette_soften.jpg`,
+  `/Users/guest1/Desktop/SAVI_QA/folders_after_folder_palette_soften.jpg`,
+  `/Users/guest1/Desktop/SAVI_QA/folders_after_folder_palette_system_dark_check.jpg`

 ## Affected Areas

diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Core/SaviCore.swift /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Core/SaviCore.swift
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Core/SaviCore.swift	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Core/SaviCore.swift	2026-05-02 18:06:25
@@ -541,6 +541,7 @@
     var order: Int
     var locked: Bool
     var isPublic: Bool
+    var usesImageBackground: Bool

     enum CodingKeys: String, CodingKey {
         case id
@@ -552,6 +553,7 @@
         case order
         case locked
         case isPublic
+        case usesImageBackground = "uses_image_background"
     }

     init(
@@ -563,7 +565,8 @@
         symbolName: String,
         order: Int,
         locked: Bool = false,
-        isPublic: Bool = false
+        isPublic: Bool = false,
+        usesImageBackground: Bool = false
     ) {
         self.id = id
         self.name = name
@@ -574,6 +577,7 @@
         self.order = order
         self.locked = locked
         self.isPublic = locked || id == "f-private-vault" ? false : isPublic
+        self.usesImageBackground = image?.nilIfBlank != nil && usesImageBackground
     }

     init(from decoder: Decoder) throws {
@@ -587,6 +591,8 @@
         order = try container.decodeIfPresent(Int.self, forKey: .order) ?? SaviSeeds.defaultOrder(for: id)
         locked = try container.decodeIfPresent(Bool.self, forKey: .locked) ?? (id == "f-private-vault")
         isPublic = locked || id == "f-private-vault" ? false : (try container.decodeIfPresent(Bool.self, forKey: .isPublic) ?? false)
+        let decodedUsesImageBackground = try container.decodeIfPresent(Bool.self, forKey: .usesImageBackground) ?? false
+        usesImageBackground = image?.nilIfBlank != nil && decodedUsesImageBackground
     }
 }

@@ -3977,7 +3983,15 @@
         scheduleSocialSyncIfNeeded()
     }

-    func addFolder(name: String, color: String, symbolName: String, image: String? = nil, locked: Bool = false, isPublic: Bool = false) {
+    func addFolder(
+        name: String,
+        color: String,
+        symbolName: String,
+        image: String? = nil,
+        locked: Bool = false,
+        isPublic: Bool = false,
+        usesImageBackground: Bool = false
+    ) {
         let trimmed = name.trimmingCharacters(in: .whitespacesAndNewlines)
         guard !trimmed.isEmpty else { return }
         var orderedIds = orderedFoldersForDisplay().map(\.id)
@@ -3990,7 +4004,8 @@
             symbolName: symbolName,
             order: orderedIds.count,
             locked: locked,
-            isPublic: isPublic
+            isPublic: isPublic,
+            usesImageBackground: usesImageBackground
         )
         folders.insert(folder, at: max(0, folders.count - 1))
         orderedIds.append(folder.id)
@@ -4004,6 +4019,9 @@
         var next = folder
         if next.locked || next.id == "f-private-vault" {
             next.isPublic = false
+        }
+        if next.image?.nilIfBlank == nil {
+            next.usesImageBackground = false
         }
         folders[index] = next
         if !folder.locked {
diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Components/AppComponents.swift /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Components/AppComponents.swift
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Components/AppComponents.swift	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Components/AppComponents.swift	2026-05-02 18:06:25
@@ -716,6 +716,7 @@
         let hex = preferredHex(for: folder)
         let base = Color(hex: hex)
         let usesLightText = usesLightForeground(for: folder, baseHex: hex, colorScheme: colorScheme)
+        let usesImageBackground = folder.usesImageBackground && folder.image?.nilIfBlank != nil
         let ink = Color(hex: "#342448")
         let paper = Color(hex: "#FFF9FF")

@@ -741,6 +742,27 @@
                 )
             }

+            if usesImageBackground {
+                return SaviFolderVisualStyle(
+                    baseHex: hex,
+                    base: base,
+                    text: paper,
+                    secondaryText: paper.opacity(0.76),
+                    titleShadow: Color.black.opacity(0.34),
+                    titleShadowRadius: 2,
+                    iconBackground: Color.black.opacity(0.18),
+                    iconForeground: paper.opacity(0.90),
+                    countBackground: Color.black.opacity(0.18),
+                    countForeground: paper,
+                    stroke: Color.white.opacity(0.22),
+                    shadow: Color(hex: "#5E4A73").opacity(0.14),
+                    pillBackground: Color.black.opacity(0.20),
+                    pillForeground: paper,
+                    pillStroke: Color.white.opacity(0.20),
+                    backgroundColors: lightBackgroundColors(for: folder.id, baseHex: hex)
+                )
+            }
+
             let text = usesLightText ? paper : ink
             let controlForeground = usesLightText ? paper : ink
             return SaviFolderVisualStyle(
@@ -750,12 +772,12 @@
                 secondaryText: text.opacity(usesLightText ? 0.72 : 0.58),
                 titleShadow: usesLightText ? Color.black.opacity(0.20) : Color.clear,
                 titleShadowRadius: usesLightText ? 1 : 0,
-                iconBackground: usesLightText ? paper.opacity(0.16) : paper.opacity(0.48),
-                iconForeground: controlForeground.opacity(usesLightText ? 0.92 : 0.86),
+                iconBackground: usesLightText ? paper.opacity(0.12) : paper.opacity(0.34),
+                iconForeground: controlForeground.opacity(usesLightText ? 0.84 : 0.72),
                 countBackground: usesLightText ? paper.opacity(0.22) : paper.opacity(0.66),
                 countForeground: controlForeground,
-                stroke: usesLightText ? paper.opacity(0.24) : paper.opacity(0.42),
-                shadow: base.opacity(usesLightText ? 0.20 : 0.16),
+                stroke: usesLightText ? paper.opacity(0.20) : paper.opacity(0.34),
+                shadow: base.opacity(usesLightText ? 0.14 : 0.10),
                 pillBackground: SaviTheme.subtleSurface.opacity(0.42),
                 pillForeground: SaviTheme.metadataText,
                 pillStroke: SaviTheme.cardStroke.opacity(0.40),
@@ -784,6 +806,27 @@
             )
         }

+        if usesImageBackground {
+            return SaviFolderVisualStyle(
+                baseHex: hex,
+                base: base,
+                text: Color(hex: "#FFF9FF"),
+                secondaryText: Color(hex: "#E8DFFC").opacity(0.82),
+                titleShadow: Color.black.opacity(0.42),
+                titleShadowRadius: 2,
+                iconBackground: Color.black.opacity(0.24),
+                iconForeground: Color(hex: "#FFF9FF").opacity(0.92),
+                countBackground: Color.black.opacity(0.24),
+                countForeground: Color(hex: "#FFF9FF"),
+                stroke: Color.white.opacity(0.16),
+                shadow: Color.black.opacity(0.18),
+                pillBackground: Color.black.opacity(0.26),
+                pillForeground: Color(hex: "#FFF9FF"),
+                pillStroke: Color.white.opacity(0.18),
+                backgroundColors: darkBackgroundColors(baseHex: hex)
+            )
+        }
+
         return SaviFolderVisualStyle(
             baseHex: hex,
             base: base,
@@ -791,12 +834,12 @@
             secondaryText: Color(hex: "#D9D0EF").opacity(0.84),
             titleShadow: Color.black.opacity(0.25),
             titleShadowRadius: 1,
-            iconBackground: Color.white.opacity(0.14),
-            iconForeground: Color(hex: "#FFF9FF"),
+            iconBackground: Color.white.opacity(0.11),
+            iconForeground: Color(hex: "#FFF9FF").opacity(0.86),
             countBackground: Color.white.opacity(0.15),
             countForeground: Color(hex: "#FFF9FF"),
-            stroke: Color.white.opacity(0.12),
-            shadow: Color.black.opacity(0.20),
+            stroke: Color.white.opacity(0.10),
+            shadow: Color.black.opacity(0.16),
             pillBackground: SaviTheme.subtleSurface.opacity(0.56),
             pillForeground: Color(hex: "#D9D0EF"),
             pillStroke: base.opacity(0.32),
@@ -861,34 +904,33 @@
     private static func lightBackgroundColors(for folderId: String, baseHex hex: String) -> [Color] {
         switch folderId {
         case "f-must-see":
-            return [Color(hex: "#8A35FF"), Color(hex: "#7430E8")]
+            return lightFolderPair("#8A35FF", "#7430E8", amount: 0.88)
         case "f-paste-bin":
-            return [Color(hex: "#9887B3"), Color(hex: "#81709F")]
+            return lightFolderPair("#9887B3", "#81709F", amount: 0.88)
         case "f-wtf-favorites":
-            return [Color(hex: "#86D9F8"), Color(hex: "#71CFF2")]
+            return lightFolderPair("#86D9F8", "#71CFF2", amount: 0.86)
         case "f-growth":
-            return [Color(hex: "#F6843D"), Color(hex: "#E1682F")]
+            return lightFolderPair("#F6843D", "#E1682F", amount: 0.86)
         case "f-lmao":
-            return [Color(hex: "#DCFF3D"), Color(hex: "#CEFA32")]
+            return lightFolderPair("#DCFF3D", "#CEFA32", amount: 0.80)
         case "f-travel":
-            return [Color(hex: "#78D3F7"), Color(hex: "#60C5EF")]
+            return lightFolderPair("#78D3F7", "#60C5EF", amount: 0.86)
         case "f-recipes":
-            return [Color(hex: "#FFC27D"), Color(hex: "#FFB06A")]
+            return lightFolderPair("#FFC27D", "#FFB06A", amount: 0.84)
         case "f-health":
-            return [Color(hex: "#83E2AA"), Color(hex: "#68D195")]
+            return lightFolderPair("#83E2AA", "#68D195", amount: 0.84)
         case "f-design":
-            return [Color(hex: "#EA6EA7"), Color(hex: "#CF4E91")]
+            return lightFolderPair("#EA6EA7", "#CF4E91", amount: 0.86)
         case "f-research":
-            return [Color(hex: "#70F1DD"), Color(hex: "#55DDC9")]
+            return lightFolderPair("#70F1DD", "#55DDC9", amount: 0.84)
         case "f-tinfoil":
-            return [Color(hex: "#8B5CF6"), Color(hex: "#6D28D9")]
+            return lightFolderPair("#8B5CF6", "#6D28D9", amount: 0.88)
         case "f-random":
-            return [Color(hex: "#FFE773"), Color(hex: "#FFD955")]
+            return lightFolderPair("#FFE773", "#FFD955", amount: 0.78)
         case "f-all":
-            return [Color(hex: "#DFFF42"), Color(hex: "#CBF42F")]
+            return lightFolderPair("#DFFF42", "#CBF42F", amount: 0.80)
         default:
-            let base = Color(hex: hex)
-            return [base, base.opacity(0.86)]
+            return lightFolderPair(hex, hex, amount: 0.86)
         }
     }

@@ -923,6 +965,62 @@
             blue: backgroundBlue + (baseBlue - backgroundBlue) * clampedAmount,
             alpha: 1
         ))
+    }
+
+    private static func lightFolderPair(_ firstHex: String, _ secondHex: String, amount: CGFloat) -> [Color] {
+        [
+            blendedFolderColor(hex: firstHex, with: "#F8F2FC", amount: amount),
+            blendedFolderColor(hex: secondHex, with: "#F3ECF9", amount: min(max(amount - 0.02, 0), 1))
+        ]
+    }
+}
+
+struct FolderTileBackground: View {
+    let folder: SaviFolder
+    let style: SaviFolderVisualStyle
+
+    private var customImage: UIImage? {
+        guard folder.usesImageBackground,
+              let imageDataURL = folder.image?.nilIfBlank
+        else { return nil }
+        return SaviText.imageFromDataURL(imageDataURL)
+    }
+
+    var body: some View {
+        ZStack {
+            style.backgroundGradient
+
+            if let customImage {
+                Image(uiImage: customImage)
+                    .resizable()
+                    .scaledToFill()
+                    .overlay(imageReadabilityOverlay)
+            }
+        }
+        .clipped()
+    }
+
+    private var imageReadabilityOverlay: some View {
+        ZStack {
+            LinearGradient(
+                colors: [
+                    Color.black.opacity(0.10),
+                    Color.black.opacity(0.18),
+                    Color.black.opacity(0.48)
+                ],
+                startPoint: .topLeading,
+                endPoint: .bottomTrailing
+            )
+
+            LinearGradient(
+                colors: [
+                    style.base.opacity(0.10),
+                    Color.black.opacity(0.18)
+                ],
+                startPoint: .topTrailing,
+                endPoint: .bottomLeading
+            )
+        }
     }
 }

diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Folders/FolderComponents.swift /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Folders/FolderComponents.swift
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Folders/FolderComponents.swift	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Folders/FolderComponents.swift	2026-05-02 18:06:25
@@ -52,9 +52,9 @@
         .clipShape(RoundedRectangle(cornerRadius: cornerRadius, style: .continuous))
         .overlay(
             RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
-                .stroke(.white.opacity(0.18), lineWidth: 1)
+                .stroke(.white.opacity(0.12), lineWidth: 1)
         )
-        .shadow(color: .black.opacity(0.05), radius: 2, x: 0, y: 1)
+        .shadow(color: .black.opacity(0.035), radius: 1.5, x: 0, y: 1)
         .overlay(alignment: .bottomTrailing) {
             if let publicBadgeStyle {
                 PublicFolderBadge(style: publicBadgeStyle)
@@ -76,7 +76,7 @@
         FolderIconBadge(
             symbolName: folder.symbolName,
             color: folder.color,
-            imageDataURL: folder.image,
+            imageDataURL: folder.usesImageBackground ? nil : folder.image,
             size: size,
             cornerRadius: cornerRadius,
             font: font
@@ -379,7 +379,7 @@
                         FolderIconBadge(
                             symbolName: folder.symbolName,
                             color: style.baseHex,
-                            imageDataURL: folder.image,
+                            imageDataURL: folder.usesImageBackground ? nil : folder.image,
                             size: SaviFolderTileMetrics.iconSize,
                             cornerRadius: SaviFolderTileMetrics.iconCornerRadius,
                             font: SaviFolderTileMetrics.iconFont,
@@ -418,7 +418,7 @@
                     maxHeight: SaviFolderTileMetrics.tileHeight,
                     alignment: .leading
                 )
-                .background(style.backgroundGradient)
+                .background(FolderTileBackground(folder: folder, style: style))
                 .clipShape(RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous))
                 .overlay(
                     RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous)
diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Home/HomeScreen.swift /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Home/HomeScreen.swift
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Home/HomeScreen.swift	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Home/HomeScreen.swift	2026-05-02 18:06:25
@@ -911,7 +911,7 @@
                     FolderIconBadge(
                         symbolName: folder.symbolName,
                         color: style.baseHex,
-                        imageDataURL: folder.image,
+                        imageDataURL: folder.usesImageBackground ? nil : folder.image,
                         size: 30,
                         cornerRadius: 10,
                         font: SaviType.ui(.caption, weight: .black),
@@ -946,11 +946,7 @@
             }
             .padding(11)
             .frame(maxWidth: .infinity, minHeight: 104, maxHeight: 104, alignment: .leading)
-            .background(
-                style.backgroundGradient
-                    .saturation(colorScheme == .light && folder.id != "f-private-vault" ? 0.94 : 1)
-                    .brightness(colorScheme == .light && folder.id != "f-private-vault" ? 0.01 : 0)
-            )
+            .background(FolderTileBackground(folder: folder, style: style))
             .clipShape(RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous))
             .overlay(
                 RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous)
diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Items/ItemViews.swift /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Items/ItemViews.swift
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Items/ItemViews.swift	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Items/ItemViews.swift	2026-05-02 18:06:25
@@ -109,13 +109,13 @@
         } label: {
             VStack(alignment: .leading, spacing: 6) {
                 HStack(alignment: .top, spacing: 6) {
-                    FolderIconBadge(
-                        symbolName: folder.symbolName,
-                        color: style.baseHex,
-                        imageDataURL: folder.image,
-                        size: SaviHomeFolderStripMetrics.iconSize,
-                        cornerRadius: SaviHomeFolderStripMetrics.iconCornerRadius,
-                        font: SaviHomeFolderStripMetrics.iconFont,
+                        FolderIconBadge(
+                            symbolName: folder.symbolName,
+                            color: style.baseHex,
+                            imageDataURL: folder.usesImageBackground ? nil : folder.image,
+                            size: SaviHomeFolderStripMetrics.iconSize,
+                            cornerRadius: SaviHomeFolderStripMetrics.iconCornerRadius,
+                            font: SaviHomeFolderStripMetrics.iconFont,
                         background: style.iconBackground,
                         foreground: style.iconForeground,
                         publicBadgeStyle: folder.isPublic ? style : nil
@@ -147,11 +147,7 @@
             }
             .padding(11)
             .frame(width: width, height: SaviHomeFolderStripMetrics.tileHeight, alignment: .leading)
-            .background(
-                style.backgroundGradient
-                    .saturation(colorScheme == .light && folder.id != "f-private-vault" ? 0.94 : 1)
-                    .brightness(colorScheme == .light && folder.id != "f-private-vault" ? 0.01 : 0)
-            )
+            .background(FolderTileBackground(folder: folder, style: style))
             .clipShape(RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous))
             .overlay(
                 RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous)
@@ -1423,7 +1419,7 @@
                 FolderIconBadge(
                     symbolName: folder.symbolName,
                     color: style.baseHex,
-                    imageDataURL: folder.image,
+                    imageDataURL: folder.usesImageBackground ? nil : folder.image,
                     size: 42,
                     cornerRadius: 13,
                     font: SaviType.ui(.body, weight: .black),
@@ -1475,7 +1471,7 @@
                 }
             }
             .padding(14)
-            .background(style.backgroundGradient)
+            .background(FolderTileBackground(folder: folder, style: style))
             .clipShape(RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous))
             .overlay(
                 RoundedRectangle(cornerRadius: SaviRadius.folder, style: .continuous)
diff -ruN /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Save/SaveAndEditSheets.swift /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Save/SaveAndEditSheets.swift
--- /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/before/SAVI/Views/Save/SaveAndEditSheets.swift	2026-05-02 18:06:25
+++ /var/folders/p2/b94vt2zd7kd1xd9q9fcdsj800000gp/T/tmp.rz52AYfPQ4/after/SAVI/Views/Save/SaveAndEditSheets.swift	2026-05-02 18:06:25
@@ -1207,6 +1207,7 @@
     @State private var color: String
     @State private var symbolName: String
     @State private var folderImage: String?
+    @State private var usesImageBackground: Bool
     @State private var locked: Bool
     @State private var isPublic: Bool
     @State private var selectedIconPhoto: PhotosPickerItem?
@@ -1230,6 +1231,7 @@
         _color = State(initialValue: folder?.color ?? "#C4B5FD")
         _symbolName = State(initialValue: folder?.symbolName ?? "folder.fill")
         _folderImage = State(initialValue: folder?.image)
+        _usesImageBackground = State(initialValue: folder?.usesImageBackground ?? false)
         _locked = State(initialValue: folder?.locked ?? false)
         _isPublic = State(initialValue: folder?.isPublic ?? false)
     }
@@ -1270,7 +1272,7 @@
                         FolderIconBadge(
                             symbolName: symbolName,
                             color: color,
-                            imageDataURL: folderImage,
+                            imageDataURL: usesImageBackground ? nil : folderImage,
                             size: 58,
                             cornerRadius: 17,
                             font: SaviType.ui(.title2, weight: .black)
@@ -1308,6 +1310,7 @@
                             if folderImage != nil {
                                 Button {
                                     folderImage = nil
+                                    usesImageBackground = false
                                     iconLoadMessage = nil
                                 } label: {
                                     Label("Use symbol", systemImage: "square.grid.2x2.fill")
@@ -1317,6 +1320,21 @@
                                 .buttonStyle(.plain)
                             }

+                            if folderImage != nil {
+                                Toggle(isOn: $usesImageBackground) {
+                                    VStack(alignment: .leading, spacing: 2) {
+                                        Text("Use as card background")
+                                            .font(SaviType.ui(.caption, weight: .bold))
+                                            .foregroundStyle(SaviTheme.text)
+                                        Text("SAVI adds a soft overlay so the folder name stays readable.")
+                                            .font(SaviType.ui(.caption2, weight: .semibold))
+                                            .foregroundStyle(SaviTheme.textMuted)
+                                    }
+                                }
+                                .toggleStyle(.switch)
+                                .tint(SaviTheme.chartreuse)
+                            }
+
                             if let iconLoadMessage {
                                 Text(iconLoadMessage)
                                     .font(.caption2.weight(.semibold))
@@ -1330,6 +1348,7 @@
                             Button {
                                 symbolName = symbol
                                 folderImage = nil
+                                usesImageBackground = false
                                 iconLoadMessage = nil
                             } label: {
                                 Image(systemName: symbol)
@@ -1446,11 +1465,20 @@
                             folder.color = color
                             folder.symbolName = symbolName
                             folder.image = folderImage
+                            folder.usesImageBackground = folderImage?.nilIfBlank != nil && usesImageBackground
                             folder.locked = locked
                             folder.isPublic = locked ? false : isPublic
                             store.saveFolder(folder)
                         } else {
-                            store.addFolder(name: name, color: color, symbolName: symbolName, image: folderImage, locked: locked, isPublic: isPublic)
+                            store.addFolder(
+                                name: name,
+                                color: color,
+                                symbolName: symbolName,
+                                image: folderImage,
+                                locked: locked,
+                                isPublic: isPublic,
+                                usesImageBackground: folderImage?.nilIfBlank != nil && usesImageBackground
+                            )
                         }
                         dismiss()
                     }
